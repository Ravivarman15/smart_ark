export { calculateLeadScore, categoryFor, type ScoreFactors, type ScoreResult } from "./leadScore";
export { statusLabel, stageIndex, canTransition, nextStage } from "./leadStatus";
export {
  LEAD_TEMPLATES,
  renderLeadMessage,
  welcomeTemplateForCourse,
  type LeadTemplateKey,
} from "./leadWhatsappTemplates";
